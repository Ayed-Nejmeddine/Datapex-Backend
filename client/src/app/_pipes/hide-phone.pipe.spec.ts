import { HidePhonePipe } from './hide-phone.pipe';

describe('HidePhonePipe', () => {
  it('create an instance', () => {
    const pipe = new HidePhonePipe();
    expect(pipe).toBeTruthy();
  });
});
